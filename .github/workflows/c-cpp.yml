name: C/C++ CI

on:
  push:
    branches: [ "magictime-miui" ]
  pull_request:
    branches: [ "magictime-miui" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
